{
  "name": "@agent-commerce-trust/sdk-js",
  "version": "0.1.0-rc.0",
  "description": "Workspace for Agent Commerce Trust Layer JavaScript packages.",
  "license": "Apache-2.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:profile-dependency && npm run build --workspaces --if-present",
    "build:profile-dependency": "node scripts/build-profile-dependency.js",
    "deprecate:deferred": "node scripts/deprecate-deferred-stubs.js",
    "lint": "npm run lint --workspaces --if-present",
    "publish:dry-run": "node scripts/pack-dry-run.js",
    "pack:dry-run": "npm run publish:dry-run",
    "test": "npm run build && node --test tests/*.test.js tests/*.test.cjs",
    "verify:ci": "npm test && npm run lint && npm run verify:publish && npm run verify:deferred && npm run publish:dry-run",
    "verify:deferred": "node scripts/verify-deferred-deprecation.js",
    "verify:publish": "node scripts/verify-publish-readiness.js"
  },
  "dependencies": {
    "@ap2-travel/profile": "file:../ap2-travel/packages/profile"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=20"
  }
}
